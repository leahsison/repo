/*

Accounts.onCreateUser(function(options, user){
				if (options.acctype)
					user.acctype = options.acctype;
				if (options.profile)
					user.profile = options.profile;
				return user;
			});
			//Router.go('dashboard');
		}
});

*/